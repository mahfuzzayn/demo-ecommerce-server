# Taste
- Prefers using the Codebase Memory MCP tools (index_repository, get_architecture, search_graph) for repository exploration and summarization tasks, explicitly requesting them by name. Fresh evidence: the works.md work-order rules required exploration via the codebase-memory index before implementing ("per the rules"), and the user's "follow rules strictly" directive made that mandatory for the Order/Activity work. Fresh evidence: when debugging a cross-boundary bug spanning a second repo (frontend client), pointed the assistant at that project by name and authorized manual exploration via the codebase MCP — "You can manually look using Codebase mcp. The project is outer of this directory and named demo-ecommerce-client" — expecting the assistant to investigate the sibling project directly rather than asking the user to paste code or fetch it. Confidence: 0.88
- Prefers to use the code-review-agent (code-review-assistant subagent) by name to review a malfunctioning module, and expects a full written diagnostic report — root cause with file/line references, prioritized problems (Critical/High/Medium), concrete code-level fixes, implementation order, and breaking-change notes — before any code changes are made ("why not working, give me a full report of the possible solution"). Confidence: 0.7
- Expects new features/modules to strictly follow the project's documented specs and existing code conventions — repeatedly instructs "follow the rules strictly" when kicking off a new module from a spec doc — most recently appended "Follow the rules strictly." to the currency + SSL auto-fill request. Confidence: 0.97
- Maintains feature/API specs as markdown docs in the repo (e.g., src/docs/api-routes.md, src/docs/v7.md) and expects them to be read (and re-read on request) before implementation begins. Confidence: 0.8
- Expects every module in the project to have a comprehensive readme.md file (overview, how-it-works, and test data with request/response examples), including retroactively adding them to existing older modules that lack documentation. Fresh evidence: asked to "Make the setting module readme file containing module details and useage guides as other module (look for context)" — the "look for context" instruction means the assistant should read an existing module's readme first and mirror its structure (e.g., coupon.readme.md: Overview, How It Works, Test Data with per-route request/response examples, Seeding, Notes) so the new file matches sibling docs. Fresh evidence: asked to update the settings readme with real data for every section so it can be sent to the frontend and tested with Postman — test data must be complete real values (not truncated `"..."` placeholders) that double as copy-paste-ready payloads per section. Fresh evidence: also expects the readme to document the exact request wire format per route — for the uniform settings `/:section` route, multipart/form-data with a required `data` JSON field + optional `images` file field (mapped positionally per section) — with per-section "how to send" instructions (what goes in `data`, where files go in Postman), and the docs verified against the actual routes/middleware so they never show a raw JSON body that would fail at runtime. Confidence: 0.92
- For PATCH updates of sub-documents that contain optional fields (especially file-upload fields with empty-string defaults like brand logo/favicon), only writes the fields the client actually provided — omitted fields must be preserved, never reset to their default/empty value ("make sure if logo and favicon is not provided then it's not modified... it should only update if user has provided it"). Implements this with dotted-path `$set` (e.g., `brand.name`, `brand.logo`) built only from provided entries instead of replacing the whole sub-document, and rejects an empty update body with a clear 400. Confidence: 0.85
- Does not want the assistant to run build/compile verification commands automatically after code changes — said "I will build when needed." Confidence: 0.8
- Communicates tersely and directively; expects immediate execution ("start working on X now", "Implement the plan") without lengthy back-and-forth once direction is given. Fresh evidence: approved the settings redesign with the two-word directive "Implement the plan" and no further commentary. Fresh evidence: explicitly asks for brief answers — "Answer shortly" — when raising design questions, expecting a concise suggestion rather than a long write-up. Fresh evidence: again ended a verification question with "answer shortly", expecting a one-line confirmation over a detailed write-up. Fresh evidence: asked to verify the CORS origin array with "is it okay, I think its okay. answer shortly" — expecting a short candid verdict that actually catches subtle config bugs (trailing slashes on production origins break the Origin header match) rather than agreeing with his own assessment. Fresh evidence: raised a production CORS question ("Do we need to mention method types in cors too? cause get working well but post, patch, delete causing problem in production") with "Answer shortly, look on app.ts file" — expecting a brief verdict grounded in the actual file that redirects away from the surface symptom (CORS methods — the cors package allows them by default) to the real cause (the Next.js "Server Components render" error is a server-to-server fetch, which is not subject to CORS at all). Confidence: 0.92
- Distinguishes authentication failures from authorization failures in middleware: a valid token presented to a route the user's role isn't allowed on must yield a clear "You are not authorized!" style 401, never a misleading "Invalid token!" — expects the role check to be evaluated step-by-step per role and custom authorization errors to pass through untouched rather than being swallowed by the token-verify catch block. Confidence: 0.85
- Proactively considers data flow and parameter transmission across system boundaries (e.g., how data arrives at each endpoint between services/gateways/frontend) before considering a feature complete; asks "how will it be receiving [data X] ensure it" to verify the contract between systems. Confidence: 0.7
- When debugging multi-step integrations where data passes across system boundaries (payment gateways, APIs, webhooks), wants a systematic step-by-step trace from initiation through each stage — "Verify from starting: Init, then ... then redirect, validate etc. step by step" — rather than isolated fixes at individual endpoints. Confidence: 0.7
- When debugging issues that persist after an attempted fix, wants the assistant to first investigate, diagnose, and explain the root cause ("find and tell me that first!") before making further code changes — diagnosis before action. Fresh evidence: after the DB-connection fix was applied and the issue was "still not working", asked "Is it client side issue then or what?" expecting a diagnosis (which proved the server was healthy and the failure was wrong test credentials) rather than more speculative changes. Fresh evidence: with the root cause confirmed to lie in the frontend repo, explicitly forbade any fixing in the current repo — "Dont fix just find the root cause and actual viable solution" — diagnosis and a solution write-up, nothing more. Confidence: 0.9
- When a production/API failure persists after fixes, asks directly which side of the system boundary is at fault ("Is it client side issue then or what?") and expects a definitive, evidence-based attribution from live testing (direct POST to the endpoint, an OPTIONS preflight, and a working GET baseline) that distinguishes a routing/connection 404 from a business-logic 404 — e.g., an auth service body like "This user is not found!" means the route matched and the DB responded, so the "failure" is just invalid test credentials, not a broken backend — and that a Next.js server-side fetch (User Agent: node) is not subject to browser CORS at all. Confidence: 0.65
- When the user pastes an external diagnosis/analysis (e.g., another AI session's root-cause report on a production 404/500) and asks "Read and let me know what do you think", expects the assistant to verify every claim against the actual code files and live endpoints before endorsing it — flagging factual errors and stale assumptions (e.g., a fix recommendation for `dns.setServers` that was already commented out in the current code, or a "stale commit" red herring) and delivering a corrected verdict with the real, condensed fix list — rather than rubber-stamping the pasted analysis. Confidence: 0.7
- When reporting a runtime error, wants it fixed AND the root cause explained in the same response — "fix this and let me know the issue" — i.e., the explanation should clarify why the error occurred (e.g., how Mongoose interprets a populate argument as a model name), not just list what was changed. Fresh evidence: reported the variant-image count-mismatch 400 with "What is the issue? what is the approach to fix this?" and the accepted response diagnosed the root cause (per-variant check against the whole remaining file pool) and fixed it in the same pass. Confidence: 0.8
- Provides IDE-context annotations (`<ide-context>`) with exact file path and line number before making requests, to give precise location context without needing to be asked. Confidence: 0.88
- When debugging a cross-boundary issue (backend API vs frontend), reports raw runtime evidence verbatim from multiple tools rather than paraphrasing — the browser devtools network panel (full request URL, method, status, remote address, payload, and response body, e.g., Next.js Flight/RSC stream markers like `"$@1"`/`"$Z"`) side by side with the Postman request/response (full JSON including tokens) — so the assistant can attribute each result to the layer that produced it (Postman hitting the API directly returns real JSON; the browser's 200 with an RSC stream body is a server-action render, not the API response). Confidence: 0.6
- Wants API route inventories documented module-by-module in the project-tracking doc (e.g., progress.md) in a specific format: module name, total route count, then each route listed with its method/type plus an inline description of what it does, and request + response formats inside code cells for every route. Expects the doc to be kept in sync as modules change — explicitly asks to "update the .md file according to the module changes" (added routes, field additions, validation notes, and the global route total all updated). Fresh evidence: again asked to "update the .md file of product module changes" right after the product module code was changed, and the resulting doc update also captured behavioral/edge-case semantics (slug uniqueness rules, isActive toggle, soft-delete 404 behavior) with notes under the route table and per-endpoint. Fresh evidence: after a round of code changes spanning multiple modules, asked to "update the .md file from our updates in Review, Product & other changed modules" — expecting the doc to be synced across all changed modules (routes, fields, behavior notes, global route totals), not just the primary one. Fresh evidence: after the user himself updated the coupon routes, asked to "update the .md file for Coupon Module" — the doc was expected to reflect the actual current route set (create/get-all/get-single/by-code/update/delete), correct the module's route total when the header was wrong (5→6), renumber section headers, and bump the global route total (52→53) so the whole doc stays internally consistent. Confidence: 0.97
- Approaches module verification in two phases: backend QA of routes first (with fixes afterward as needed), then a pass ensuring important edge cases pass for each module's routes and that code quality is readable, manageable, and reusable. Confidence: 0.7
- Is token-cost conscious; when asked to use token-saving approaches (e.g., Codebase Memory MCP), expects a quantified report of estimated tokens saved at the end of the task. Confidence: 0.7
- Prefers image/file uploads to use Cloudinary via multer middleware (`multerUpload.single()`, `parseBody`) rather than accepting raw URL strings in the request body. When adding upload support to a module, expects validation schemas to exclude fields that will be provided via file upload instead. Confidence: 0.8
- Expects the assistant to implement only what was explicitly asked for — does not want extra routes, handlers, or endpoints added without being requested. Pushes back sharply when assistant introduces unrequested functionality (e.g., separate success/fail/cancel callback routes that were not part of the original requirement). Confidence: 0.9
- Has clear architectural decisions already in mind and expects the assistant to follow that design rather than proposing or implementing alternative approaches. When a single-endpoint pattern (validate + redirect) is already established, all callbacks should flow through that same endpoint rather than being split into separate routes. Confidence: 0.8
- For Stripe integrations, prefers to maintain both PaymentIntent and Checkout Session implementations alongside each other in the codebase ("seperate both and keep both features but activate the checkout system"), keeping one active while the other is preserved for potential future use. Confidence: 0.8
- When a redirect-based payment gateway route pattern (success/cancel callback routes) was previously working and validated, prefers to restore that known-good pattern rather than redesign the integration — "the previous thing ... was working good previously add and make sure validation will work without any issue." Confidence: 0.7
- When implementing payment gateway integrations, expects webhook/callback signature verification to be included from the start — explicitly requests "make sure validate webhook works" as a requirement alongside the main implementation, not as a later add-on. Confidence: 0.6
- When a payment integration uses frontend SDK methods (e.g., Stripe PaymentIntent with confirmCardPayment), prefers the backend as a pure JSON API layer without redirect callback routes. However, when using a redirect-based gateway (e.g., Stripe Checkout Sessions, SSLCommerz), expects dedicated server-side success/cancel callback routes to handle the gateway redirect and forward the user to the frontend. Preference is integration-model-dependent, not absolute. Confidence: 0.6
- Wants compact session handoff summaries when starting a new chat, organized by fixed categories: folder structure, file naming convention, code writing style, what has been completed, what is next to do, plus "other necessary things" — important context the assistant should proactively include beyond the listed headings (e.g., known mismatches/flags, git status, recent commits). Confidence: 0.8
- Wants session handoff summaries persisted as markdown files in the project docs folder (e.g., src/docs/project-tracking/summary-cmdc-chat.md, and most recently src/docs/project-tracking/summarized.md) so a new chat can be initialized with full context by reading that file — explicitly asked to "add to the summary-cmdc-chat.md file" after the summary was produced in-chat, and later to "write on to summarized.md file"; the summary should also capture recent commits and git status so a new session knows nothing is assumed committed. Fresh evidence: opens each new session by directing the assistant to read the handoff summary first and grasp the context before any tasks are handed over ("Read the summarized.md file and grasp the context, then I will pass the tasks") — the summary read is the session-init ritual, and tasks only come after context is confirmed loaded. Confidence: 0.85
- For update endpoints, prefers validation schemas where every field is optional (proper update semantics) rather than reusing create-style schemas with required fields — explicitly asked to "create another update schema where zod is optional as it's update." Confidence: 0.8
- Prefers consolidating overlapping validation schemas into a single schema per operation instead of maintaining multiple partially-duplicated schemas ("merge validation into one"). Confidence: 0.7
- Expects validation schemas, the Mongoose model, and the TypeScript interface to stay in sync within a module — fields that are validated but missing from the model/interface are treated as a bug ("model needs info details added"), and when a new property is added to a model the user explicitly expects the validations to be updated alongside it ("needs description property on the model, also update the validations"). Confidence: 0.7
- When the user proposes an approach and explicitly asks for assessment before implementing ("is this a good approach?" / "assess first if my idea is best"), wants a candid assessment first — agreement/disagreement with reasoning and edge cases (e.g., count/list mismatch, role consistency) — and explicitly wants scalability/future-maintainability addressed ("is this scalable and won't make issues?"). When the user pre-authorizes ("if so then we apply, otherwise just improve if needed"), the assistant may apply the original idea or an improved version directly after the assessment, without another confirmation round. Fresh evidence: user asked "Should we implement it, and is this scalable and won't make issues?" before committing to the payment-props design. Fresh evidence: before adding the currency field, asked "let me know should we add currency or not" — wanting a candid recommendation with reasoning (multi-client reusability) before committing. Fresh evidence: asked "before updating tell me is this efficient best standard?" ahead of the invoice `?by=transactionId` change — expecting a candid efficiency/best-standard assessment with collision-safety reasoning before any code was written. Fresh evidence: asked "I want to understand is the current process of us is better and is good?" — seeking an honest verdict on the existing currency-snapshot design (trade-offs: price semantics preserved + history intact, vs. the live-switch weakness: mixed-currency catalog split, no price conversion), not just confirmation, before deciding whether to change anything. Fresh evidence: asked "What will happened then when currency changed to usd from bdt and product price updated will it then update the currency with price? Should it work like this?" — probing whether the current behavior (price edits leave currency untouched, explicit `currency` accepted on PATCH but undocumented) is the right design and expecting a candid verdict with the clean-fix recommendation (deliberate bulk currency-switch endpoint vs. documenting the existing per-product field) before committing to any change. Confidence: 0.93
- Tests and verifies modules sequentially, one module at a time — declares the current module complete ("User module testing completed for now") before moving on to the next module's testing ("We have moved to Auth module testing"). Confidence: 0.6
- Expects zod request-validation schemas wired via validateRequest on every API route in a module, not just the primary ones — when asked to add a schema for one route (e.g., /auth/login), also wants the remaining routes' missing schemas covered ("insure other necessary route validation schema needed are safely added"). Confidence: 0.6
- For security edge cases (e.g., change-password where the new password must differ from the old one), wants the rule enforced in the zod schema (`.refine`) and defensively in the service layer, and expects the validation to actually catch it ("password cannot be same, validation must pass ensure these"). Confidence: 0.6
- For unique fields like slugs, prefers them optional on input: when not provided, auto-generate from the name (lowercased/hyphenated) plus a short random suffix so uniqueness is guaranteed, and expects the generator to live in a shared utils file (e.g., `src/app/utils/generateSlug.ts`) rather than inline in the model/service. Confidence: 0.8
- Explicitly asks to "maintain all types of edge cases" when implementing features — expects proactive handling of conflict/collision scenarios (duplicate names, slug collisions, uniqueness checks on create and update) during implementation, not just happy-path behavior ("Make sure all types of edge cases where maintained"); expects update operations to validate every changed field and check for conflicts (code/name conflicts, excluding self). Fresh evidence: repeated the exact instruction "Must maintain all types of edge cases" when kicking off the order module rework, and again said "Ensure all types of edge cases where maintained well" when adding payment fields/guards. Fresh evidence: approved the product-color redundancy fix with "so maintain edge cases" bundled into the same directive as the fix + docs update. Confidence: 0.96
- When fixing a bug that touches a cross-cutting concern (e.g., slug handling, soft-delete, status toggling), expects the fix to be applied consistently across the whole module — validation schema, model, interface, service logic, and read queries — not just at the spot where the error surfaced ("maintain edge cases, and update across the whole module"). Confidence: 0.6
- Expects each module to carry the full standard CRUD route set including a get-single `GET /:id` route — when a single-get route is added to one module (e.g., brand), the same pattern is expected to be replicated in subsequent modules (e.g., category) without being re-specified in detail. Confidence: 0.6
- Prefers route params to be named consistently across a module — e.g., use `couponId` on all id-based routes rather than mixing `couponId` and `couponCode` param names; explicitly said "routes should maintain consistency of params using, couponId only not couponCode." Confidence: 0.8
- Prefers soft delete over hard delete: expects an `isDeleted` boolean flag (default `false`) on the model/interface, with delete operations setting ONLY that flag and nothing else — explicitly said "Delete will only make isDeleted True" (no side effects like also deactivating `isActive`) — instead of physically removing records. Public read queries (list and get-single) filter out deleted records, which then return 404. The same applies to content hidden by other flags: public reads (including populated references) must filter to `isActive: true` only, so admin-hidden records (e.g., deactivated reviews) never leak. Confidence: 0.8
- Distinguishes admin-management reads from public-consumption reads for records with time-based validity (e.g., coupons): admin list/single views surface all non-deleted records — expired or inactive included — so admins can manage them by id, while public validation routes (e.g., get coupon by code) stay strict and reject not-yet-active/expired/inactive coupons. Only soft-deleted records are hidden from admin views. Confidence: 0.8
- Prefers toggling boolean status fields (e.g., `isActive`) through the existing generic update route (PATCH) rather than via a dedicated toggle endpoint. Confidence: 0.6
- When adding reference fields used purely for audit/ownership tracking (e.g., `createdBy`), prefers them stored as plain ObjectIds and explicitly NOT populated in API responses — "add it but not necessary to populate" — to keep responses lean and avoid unnecessary joins/lookups. Confidence: 0.7
- For moderating user-generated content (e.g., reviews), prefers a moderation flag (`isFlagged`, default false) over an active/inactive status — explicitly asked to "change the isActive with isFlagged and remove isActive property" rather than keep both. Flagged items are hidden from public reads and excluded from aggregation/rating calculations, and the admin toggle flips the flag; entity-level `isActive` fields on other models are unrelated and stay. Confidence: 0.6
- When requesting feature work on an existing module, wants a written quality/issues report on the current module FIRST — "First report me about the order module quality and issues if it has. Then..." — before any code is written, so the identified problems and the implementation plan are explicit up front. Fresh evidence: for the product-variant color redundancy, asked "First report me is current system is good or this is a bug or issue then provide me the fix then I will read and approve" — the report must cover whether the current design is good or buggy plus the proposed fix, and implementation is explicitly gated on the user reading/approving the report. Confidence: 0.8
- For e-commerce order flows, expects server-authoritative validation and computation: products must be checked (exists, active, not deleted, sufficient stock), coupon codes verified (exists, active, in date range, meets min-order) with the discount computed per coupon rules, and monetary fields (totalAmount, discount, deliveryCharge, finalAmount) computed server-side from DB prices — never trusting client-sent unitPrice/amounts (a client could otherwise set finalAmount to 1). The update flow must follow the same strategy as create (re-validate products, re-verify coupon, recompute final amount) — "When updating order it work similar to creating route strategy." Confidence: 0.75
- For generic payment-tracking fields (sessionId/transactionId), prefers storing the RAW unprefixed gateway values — no `stripe_...`, `ssl_...`, `bkash_...` prefixes — because prefixed ids won't match the values the gateway sends back on callbacks (e.g., SSL's raw `tran_id`) and cause lookup mismatches; the `paymentProvider` field already identifies which provider was used ("keep the transactionId consistent as remove prefix as it won't match with stripe or ssl or bkash and can cause trouble and paymentProvider we can know easily what provide is used"). This reversed an earlier prefix convention. When a single generic pair can't cover a gateway's needs (e.g., SSL needs both a gateway session key and a transaction id), accepts a refined set of provider-role-specific fields (stripeSessionId, sslSessionKey, transactionId). Confidence: 0.8
- For order read endpoints (get-single order), expects authorization restricted to admin OR the order's owner — "only admin or the user who ordered can get" / "not any user." Ownership checks must include the admin exemption (e.g., `role !== "admin" && order.user.toString() !== userId`), never a bare owner-id comparison that blocks admins; and when the owner reference is populated (e.g., `populate("user", ...)`), the check must compare against the raw owner id captured before population — `toString()` on a populated doc (e.g., `"[object Object]"`) silently fails for the owner. Fresh evidence: user reiterated the requirement when the check still blocked the owner. Confidence: 0.9
- Wants multi-currency support built into the data model for reusability across international and local client projects: a shared `Currency` enum (usd, bdt, eur, gbp, inr, aed, aud, cad), products default to `usd`, and orders inherit the currency of their products (mixed-currency orders rejected), with a future store-level currency/dynamic system in mind — "this a project for our clients (international or local), reusable it should be." Confidence: 0.7
- For payment init when the order's currency is unsupported by the gateway (e.g., Stripe does not support BDT), prefers a hybrid currency strategy: charge in the order's native currency directly when the gateway supports it, otherwise auto-convert `finalAmount` to a gateway-supported currency (e.g., BDT→USD) via a free FX API (open.er-api.com, no key) at init time, round to 2 decimals, and persist the conversion metadata (`fxRate`, `fxBaseCurrency`) on the order for reconciliation/auditability — never charge a non-supported currency and never silently charge in the wrong currency. Charge amount AND charge currency are always derived from the order, never client-supplied (the Stripe init body was made empty so the client can't override currency) — "it should use the currency or convert." Chosen explicitly by the user from the "Hybrid convert" option. Confidence: 0.8
- Prefers payment-init (e.g., SSLCommerz) to auto-fill customer/shipping details from the order's user profile (name, email, phone, address, city, state, postcode, country) so the client does not have to enter those fields — none of the customer fields are required in the request body (overrides still allowed): "should be done automatically from user details so user don't need to entry those input details fill all fields relatively." Confidence: 0.7
- Prefers shared constant data (enums/static values used across multiple modules, e.g., the `Currency` enum) to live in a dedicated `src/app/constants/` folder as plain constant data, NOT as a module under `src/app/modules/` — explicitly said "dont keep currency as a module inside modules folder keep inside constants folder as constant data." The old module directory is deleted and all cross-module imports point at the constants file. Confidence: 0.9
- For major module redesigns, presents the target design up front as a detailed spec and expects a written implementation plan WITH pros and cons before any code changes — explicitly asked "Then let me know the plan. Pros and cons." after providing the singleton-settings architecture. Fresh evidence: on the seed-data restart asked to "prepare the plan again" before any regeneration — a fresh written plan (collection inventory, ObjectId strategy, validation approach, deliverable locations) is expected as the first step of restarted work, with the assistant checking the current state of the output directory before planning a clean slate. Confidence: 0.8
- When given an external design spec (e.g., a module architecture spec pasted from Claude AI), expects it treated as guidance that must be adapted to the project's own code-writing system and naming conventions, not implemented verbatim — "must maintain current project code writing system and naming convention etc. just learn and undestand." Recognized adaptation points: plural `*.routes.ts` filenames, singular `XxxController`/`XxxServices` exports, model as `export default` via `mongoose.model<ISettings, SettingsModel>`, `PATCH` (not PUT) for update routes, `auth(UserRole.ADMIN)` from `user.interface` (no `USER_ROLE` constant file), `validateRequest` parsing only body/cookies (so `:param` enum guards become custom middleware and per-section body schemas are picked via a `Record<Section, ZodSchema>` map), `config.db_url` for seed scripts, and seed scripts under `src/scripts/` with `npx ts-node` npm scripts. Confidence: 0.8
- Prefers keeping related configuration/data areas as typed sub-documents on a single document (e.g., a singleton settings doc with `{ _id: false }` per-section schemas) rather than separate collections/modules, only promoting a section to its own collection when its access pattern genuinely diverges (needs pagination, independent versioning, or unbounded growth) — explicitly stated "not preemptively" as the rule going forward for new config areas. Fresh evidence: the settings module spec restated the rule — new config areas become new sub-schema fields on the existing model, and arrays-of-many content records (posts, products, promo banners) belong in their own module/collection, never on the config document. Confidence: 0.8
- Prefers seeding singleton/idempotent records via an explicit seed script run once at deploy time (npm script, CI-CD) rather than lazy `findOrCreate` on first request — the settings spec states seeding at deploy time avoids a race condition on duplicate `_id` and requires the script to be idempotent ("Settings singleton already exists, skipping"). Confidence: 0.8
- Prefers `optimisticConcurrency: true` on Mongoose schemas that multiple admins write to concurrently (e.g., the singleton settings doc) to guard against silent last-write-wins on concurrent edits. Confidence: 0.6
- When a module uses a cache, expects all writes to go through the service layer so cache invalidation is guaranteed — "never bypass the service layer for writes." Confidence: 0.6
- For storefront-facing config routes, expects the read endpoint (GET /settings) to be public/unauthenticated since the frontend needs it to render, while write endpoints (PUT /settings/:section) require the admin role via the existing auth middleware. Confidence: 0.7
- Prefers a minimal in-memory cache (a tiny cache module with get/set/invalidate) over adding a Redis dependency, but expects the multi-instance staleness limitation to be documented with a note to swap to Redis/pub-sub or a short TTL when scaling beyond a single instance. Confidence: 0.6
- Documents architectural rules as inline comments in source files (e.g., a `// RULE:` comment in settings.interface.ts stating that settings holds singleton site-wide config only and that list-growing content belongs in its own module/collection). Confidence: 0.5
- Shares reference/source files (e.g., theme.config.ts, global.css) for the assistant to study before planning, so the plan derives exact data shapes from them — "i have imported the theme.config.ts file, so you can learn, also global.css." Confidence: 0.6
- When a previous plan was too confusing to understand, prefers the assistant to start over from zero with a fresh, clearer plan rather than clarifying/iterating on the old one — "the previous plan of your I didn't understand so work from zero again." Confidence: 0.6
- Prefers extending an existing working approach over rewriting it — "the current approach is good but needs some changes" — expecting incremental additions (new typed sections, richer fields) that keep the established pattern intact. Confidence: 0.6
- Prefers single-sourcing shared data (e.g., brand logo stored once under `brand`, with navbar/footer referencing it instead of storing their own copies) so shared assets stay in sync — "Footer (synced logo with brand which will be used for navbar too)." Confidence: 0.6
- Expects feature/flow explanations to be written into the project-tracking `works.md` doc in a simple, numbered-step format — e.g., how the settings in-memory cache works (first request hits the DB, subsequent requests served from memory, update invalidates the cache) — including production-deployment behavior (Vercel serverless = per-instance best-effort cache, Redis only when scaling to multiple instances). Explicitly asked "Write down the flow in works.md file. simple way." Fresh evidence: asked to "explain in detail with and without variant product module works in works.md file bottom section add content" — a detailed explanation of how the module behaves in both modes, appended at the bottom section. Confidence: 0.7
- Extracts module documentation as a frontend-consumable data contract (e.g., settings.md) that can be handed to the frontend team directly: complete document shape, every section's exact default values as copy-paste-ready JSON seeded from the real presets (e.g., perfume_oil + theme oklch tokens), the API contract (routes, response/error shapes like the 404 "not seeded" case), and a static-defaults strategy — so the frontend can build static data mirroring the backend shape and set per-section defaults before live data arrives. Explicitly framed the doc as the thing he will "pass this document format and context to frontend." Confidence: 0.7
- Wants backend-down resilience designed and written into the docs: the frontend keeps static settings data so that if the backend fails it renders that data instead of breaking. The documented strategy should cover a short fetch timeout, silent fallback to static defaults on any failure (network/timeout/404/500 — no error screen), deep-merging live data over defaults per section so missing keys never break the UI, and a re-try interval to upgrade back to live data once the backend recovers. Confidence: 0.7
- When reviewing a data-contract document handed off to the frontend (e.g., settings.json / settings.md), expects the assistant to verify it against the actual backend implementation — schema/interface, validation, presets, and asset availability — and report which edge cases it passes plus anything still needing work (e.g., a doc default diverging from the seeded backend default like theme.dark.enabled, or /demo asset paths the backend does not actually serve) rather than accepting the document at face value. Fresh evidence: repeated the same framing nearly verbatim for settings.json — "Read the file and let me know all types of edge case passes. Anything we need to work on?" — which prompted checking asset availability and flagging the dark-mode default divergence. Confidence: 0.7
- When wrapping up / ending a phase of work (e.g., "Lets end SERVER here"), expects a final documentation close-out before closure: audit and update EVERY module readme to match the actual current code (routes, fields, behaviors — including modules not recently changed, marking any that are already current as verified), refresh the top-level usage guide, and finalize the tracking docs — progress.md (route tables/totals, QA notes marked resolved) and works.md (set an "Ended at" date and add a wrap-up entry listing what was synced and remaining known limitations). Known limitations are acceptable to leave documented for a later bug-fix round rather than fixed before closure. Fresh evidence: repeated the exact close-out trigger verbatim — "Lets end SERVER here" — with the same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
- For API changes, prefers minimal, additive, backward-compatible designs: when a route needs to also support a new lookup key (e.g., invoice by transactionId), add an optional query param (`?by=transactionId`) and keep the existing param/behavior (orderId/_id) as the default path so existing callers keep working with zero changes — explicitly framed as "keep it minimal." Confidence: 0.7
- When maintaining documentation alongside code changes, prefers incremental in-place edits over rewriting files from scratch, explicitly to conserve tokens — "don't waste tokens and write from scratch, just update" — and is watchful that a doc update never creates a new/duplicated file instead of editing the existing one ("did you created a new backend-guide file or just inserted? ... I said to you to just update the file"). Confidence: 0.85
- Whenever a module is updated, expects the module's own readme AND the backend-usage-guide.md to be updated in the same change — "Make sure when updating module you update the corresponding readme file and backend-usage-guide file" — as an ongoing rule applied to every module change, not only at phase close-out. Fresh evidence: bundled the same requirement into the invoice route change — "Update it and the update the module readme and backend-usage-guide." Fresh evidence: the currency field-shape change task came with the same paired requirement — "update it then readme and backend-usage-guide." Fresh evidence: when adding a missing niche (perfume_oil) to the theme presets, the user bundled the docs requirement into the same request — "And then update if readme and backend guide needs update specifically" — leaving the necessity judgment to the assistant but expecting every niche reference in both docs (theme table, valid-niches list, overview bullet) to be synced in the same pass. Confidence: 0.97
- When modeling config/theme data from a reference file the user provides (e.g., global.css), expects the schema to expose the FULL set of available options (all CSS tokens: base + chart + sidebar + fonts.mono) rather than a curated subset, so the client can send only the fields it wants to change (partial update) while unprovided tokens are preserved. Confidence: 0.75
- After an API/schema update, expects a concise "what to tell the frontend" handoff note in the response — changed field names (with the camelCase mapping from CSS variables), what's new, and the partial-update semantics — so the user can relay it to the frontend team without re-deriving it. Fresh evidence: asked for the exact wording to hand to the frontend command-code assistant — "tell me what to tell my frontend command code assistant exactly to lookup the changes from the backend guide file and make upate in the frontend" — so the handoff should be a copy-paste-ready prompt directing the frontend assistant to read the backend-usage-guide.md (the source of truth for the API) and apply the enumerated changes. Confidence: 0.8
- For multi-niche/multi-preset storefront systems, expects each niche preset to carry its own fully-populated theme — complete light + dark color palettes, font pairing, and radius filled for every field (not empty-string defaults) — and the theme to be distinct and dynamic per niche, so applying a niche preset swaps the whole look, not just the content sections. Explicitly: "Update the theme preset with each section filled, and the theme should be different and dynamic according to the niche." Confidence: 0.8
- Supports guest checkout in e-commerce order flows: order creation must work without authentication (optional-auth middleware — absent token → guest, present-but-invalid token still rejected), guest orders get the same random unguessable `orderId` as user orders (no user/guest suffix — the old `U`/`G` suffix convention was removed when orderId became random) and required recipient fields (`recipientName`, `phoneNo`, optional `notes`), and a public track-order route so guests can follow their order without an account. Guest checkout extends through payment: payment-init and payment-validate routes (stripe/init, sslcommerz/init, bkash/init, bkash/validate) must be public — no auth gate — since guests initiate and complete payment ("init payments providers should be no auth as we are allowing guest or non logged user to initiate payment"). Guest access also extends to the order invoice: `GET /:orderId/invoice` must be public (no auth middleware) so non-logged-in guests can download the invoice ("guest user want to download the invoice too"); when the controller doesn't use `req.user`, plain removal of the auth middleware is the correct choice — `optionalAuth` is NOT guest-friendly because it rejects requests carrying stale/invalid tokens (jwt.verify throws → 401). Confidence: 0.7
- After a production bug's root cause is proven to lie elsewhere (e.g., the frontend client), prefers rolling back the speculative debugging changes made to the server code rather than keeping them — explicitly asked "should we rollback the server.ts to previous state and remove what we add?"; the accepted recommendation was `git checkout src/server.ts` to restore the committed state (zero blast radius), keeping only improvements that are deliberate and separately committed later. Confidence: 0.7
- When asked whether a boot-time maintenance/migration function (e.g., `migrateProductIndexes` dropping a legacy index) is still needed and whether it should run on every boot, expects a candid verdict — keep it only if it's a real (idempotent) bug fix, note the per-boot cost, and offer a one-time `src/scripts/` migration as the cleaner alternative; explicitly asked "do we need that function and calling each time needed? let me know." Fresh evidence: after rolling back server.ts, asked "where to call the migrateProductIndexes() function? I think for now keep it disabled? or will it cause issue?" — expecting the correct placement confirmed (inside `bootstrap()` right after `connectToDatabase()`, once per boot) and a definitive risk verdict on disabling: keep it enabled while the legacy index might still exist in production (disabling risks the duplicate-key bug returning), safe to remove only after verifying in Atlas that the old index is gone — no action needed until then. Confidence: 0.7
- For publicly exposed record identifiers (e.g., the order `orderId`), prefers random, unguessable formats with NO encoded date and NO incrementing sequence/numbering — a predictable scheme lets users guess other records and scrape data, which the user explicitly called risky. Final format is `DEXXXXXXXX` — `DE` + 8 cryptographically random alphanumeric chars with NO hyphen (user removed the hyphen himself: "no hyphen needed", e.g. `DEY2H7ULPD`) — and expects docs/comments to state it's unguessable. Confidence: 0.9
- For cross-cutting audit needs, expects a dedicated activities/audit-log module: every important write across modules (orders, brands, categories, products, reviews, users, coupons, payments, settings) logged by the service layer (`module`/`type`/`message`/`referenceId`/`reference`/`performedBy`/`metadata`), admin-only read + clear routes (clear-all, clear-by-date-range, clear-single; no delete route), and fire-and-forget logging outside the main write's transaction so the log never blocks or rolls back the primary write. Confidence: 0.6
- For public, unauthenticated lookup endpoints (e.g., track-order), expects matching to use ONLY the human-friendly business key (`orderId`) and explicitly NOT accept the Mongo `_id` — the customer enters their `orderId` on the tracking page, so it must be the stable public key. A previous `$or: [{ orderId }, { _id }]` fallback is tightened to `{ orderId }` only and docs state `_id` is not accepted ("The track order should be tracked by orderId not _id"). Contrasts with internal/admin routes where the `_id` fallback is fine to keep. Confidence: 0.8
- For e-commerce review modules, expects review creation to be gated by genuine-purchase verification plus duplicate protection — the two required validations: (1) look up the user's orders for the product and only treat `Processing`/`Shipped`/`Completed` orders as a verified purchase (`Pending`/`Cancelled` orders do NOT count), setting `isVerifiedPurchase` from that real lookup instead of a placeholder `false`; (2) keep the one-review-per-user-per-product duplicate check. Fresh evidence: the verified-purchase rule was later hardened into a HARD gate — `createReview` must reject with 403 ("Only customers who purchased this product can review it!") when the customer has no verified purchase for the product, so only verified purchasers can review at all and `isVerifiedPurchase` is always `true` on created reviews; the new rule and its error were also reflected in the module readme and backend-usage-guide in the same pass ("make sure user must made verifiedPurchase, so we only allow verifiedPurchase review"). Also expects a customer self-service `GET /review/my-reviews` route (their own reviews, including flagged ones) so customers can view/use them, mirroring the existing `my-orders` pattern. Confidence: 0.7
- Uses `src/docs/project-tracking/works.md` as the task-assignment mechanism: writes batches of tasks under numbered "Work N" sections and kicks off implementation by pointing at a specific section/line ("Written the tasks on Work 2 line 89 read and start executing") — the assistant is expected to read that section and execute every task from it in one pass. Fresh evidence: kicked off the product-module bug-fix round with "Now I want you to follow rules of works.md file" followed by 4 numbered tasks (fix create/update, harden the variant system, update the readme, append a detailed explanation to works.md) — the doc is the source of rules AND the list of work to execute. Confidence: 0.75
- When assigning a multi-part task (fix a bug, evaluate a data-model design, update docs, explain the flow in works.md), expects all parts executed in one pass and summarized together at the end — no pausing between subtasks. Confidence: 0.7
- When the user expresses a design concern explicitly framed as "is this scalable and will it manage easily?" (e.g., the product variant system with color/size/custom-name variants), expects the assistant to assess/answer that question in the delivered work — the design validated (attribute axes as the generic mechanism + enforcement that variant keys/values draw from declared axes) and the assessment stated in the summary — not just implemented blindly. Confidence: 0.7
- When a documented API flow is currently rejected by validation (e.g., update docs say send only `{ publicId, order }` for variant images but the schema requires `url` with `min(1)`), treats the schema as the bug and relaxes it so the documented behavior becomes valid (image `url` made optional with default), rather than changing the docs to match the strict schema. Confidence: 0.6
- Verification/QA is a user-driven loop: after the assistant implements, the user will verify and test themselves and will tell the assistant if updates are needed ("complete the task then I will verify and test and if update needed I will tell") — the assistant should finish the whole task in one pass rather than pausing for step-by-step confirmation. Fresh evidence: when approving a fix, explicitly authorized direct application with the understanding that he will deploy and test it himself — "Please apply the fix, currently we can do trial and error phase. I will then deploy and test" — and framed the current stage as an acceptable trial-and-error iteration phase, so fixes should be applied directly and iterated on rather than over-engineered for completeness up front. Confidence: 0.7
- When the user pastes a canonical expected document/JSON shape for a model and says "we missed something, find out why?", treats that pasted document as the authoritative contract for the model: the assistant is expected to diff it against the current implementation (interface, model, validation, service, docs), identify every missing/divergent field, implement the gaps across the whole module, update the necessary docs in the same pass, and verify the related edge cases end-to-end ("make sure it works seamlessly for main images storing, variant images storing, stocks etc." — including cross-module consumers like the order module's stock decrement) — while explicitly flagging sample values that conflict with the deliberate existing design (e.g., legacy uppercase currency or string imageUrls) instead of silently regressing them. Confidence: 0.7
- When the assistant surfaces a potential issue or inconsistency discovered during work (e.g., settings `brand.currency` being a free-form string that could violate the product model's lowercase `Currency` enum; variant `imageUrls` being plain `string[]` while main `imageUrls` are `{publicId,url,order}`), the user asks for a short explanation of the issue, whether an update is actually needed, and how the two things relate — "Tell me what is the issue on currency in lowercase, do we need any update? Then how variants imageUrls will same work as imageUrls main section wise? answer shortly." — expecting a concise recommendation with an explicit "want me to do it?" offer, and authorizes changes only after that exchange. Fresh evidence: when the user challenges the claim by pointing at code that appears fine ("Before first explain where uppercase currency and lowercase currency cause I see everything okay!" — IDE-context pointing at payment.utils.ts where `usd` is handled case-tolerantly), the assistant must trace the data flow end-to-end and demonstrate exactly which layers are case-tolerant (e.g., `toLowerCase()`/`toUpperCase()` normalization, Stripe's lowercase expectation) versus the one unguarded handoff that isn't (settings free-form string → product/order lowercase enum), and state whether a fix is actually needed — all BEFORE making any change. Fresh evidence: the user approved immediately after the traced explanation — "Okay apply that, still I haven't understood cause I see all are lowercased" — authorizing the recommended fix even while admitting he didn't fully grasp the reasoning, then bundling follow-on work into the same message. Confidence: 0.8
- When a module stores images for main records and for nested sub-records (e.g., product main `imageUrls` vs `variants[].imageUrls`), prefers the SAME structured shape and management behavior everywhere — explicitly asked to "apply the imageUrls same behavior for variants" so variant images become `{publicId, url, order}` Cloudinary-tracked objects identical to main images (with `url` backfill by `publicId` and sequential `order` on update), not plain URL strings. Confidence: 0.85
- For boolean feature toggles that gate stored sub-data (e.g., product `hasVariants` gating `variants[]`/`attributes[]`), expects non-destructive OFF semantics: flipping the flag to `false` alone must PRESERVE the associated data so the admin can re-enable losslessly later, and data is only cleared when the client explicitly sends an empty array — "turning off hasVariants to false should we delete the variants data and attributes for sizes? I think we should not cause admin may re enable. if it is not standard then fix." The auto-set logic must only run when the data field is actually sent, so a bare flag toggle never wipes it. Confidence: 0.8
- When a shared enum exists for a field (e.g., the `Currency` enum), expects it enforced at every write boundary — the settings model (`enum`) and validation (`z.nativeEnum`) included — so free-form/uppercase strings like `"USD"`/`"EURO"` can never enter and silently break downstream consumers that validate against the same enum (product/order creation). Approved this constraint for settings `brand.currency` after the case-mismatch handoff risk was traced. Confidence: 0.7
- After declining proposed changes to a working design, wants the assistant to stop offering implementation options and answer only the specific question asked — "For now keep it as it is. we'll go forward and for you just answer: ..." — a direct answer with no new proposals, no re-offering of the previously declined feature. Confidence: 0.7
- Prefers to keep a defensible working design unchanged and move forward rather than implement assistant-proposed enhancements: after the candid assessment confirmed the currency-snapshot design (products inherit currency at create time) serves its purpose for new stores, explicitly declined the bulk currency-switch endpoint and kept the behavior as-is. Confidence: 0.6
- Keeps service files lean by extracting helper/utility functions (including DB-touching helpers like product validation/pricing and coupon application) into the module's own `*.utils.ts` file — explicitly asked to "Move utility function from service file to util.ts for order module", expecting the helpers consolidated in `order.utils.ts` alongside the existing `resolveDeliveryCharge`, imported by the service, with now-unused imports removed from the service. This is per-module organization (a `{module}.utils.ts` beside the service), distinct from shared cross-app utils in `src/app/utils/`. Confidence: 0.7
- Expects changes to be confined to the exact file(s) explicitly named in the request — when a docs task said to work on the product module's `product.readme.md`, the user pushed back that the main `README.md` had not been authorized: "I did't said to update README.md file but on product module product.readme.md file just work in here and undo readme.md file to previous state." Any file touched beyond the named scope must be reverted to its previous state (byte-identical via git checkout, not approximately restored). This extends the "implement only what was asked" rule to the file level. Fresh evidence: after the revert, the user confirmed the boundary again before authorizing the next step — "README.md file is back on previous state, now work on product.readme.md file" — i.e., the out-of-scope file must be confirmed restored before work proceeds, and the new work is confined strictly to the named file. Fresh evidence: the scope here also covers which concerns the fix may touch — the user's "get routes are working well, but post routes causing troubles" pointed the fix at the DB-connection layer shared by all routes (the real common cause) rather than CORS or the routes themselves; the accepted fix stayed confined to the connection layer in server.ts. Confidence: 0.92
documented for a later bug-fix round rather than fixed before closure. Fresh evidence: repeated the exact close-out trigger verbatim — "Lets end SERVER here" — with the same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
he same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
nfidence: 0.8
usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
 finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
nce: 0.8
sage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
ogress.md/works.md) before moving to the frontend. Confidence: 0.8
 finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
nce: 0.8
sage-guide.md, finalize progress.md/works.md) bef- Prefers shared, cross-module helper functions (e.g., Cloudinary image-destroy) to live in a GLOBAL location — `src/app/config/cloudinary.config.ts` — so any module can reuse them, with module-specific files re-exporting for convenience — "Make sure cloudinary image destroy functions is a global function keep in cloudinary config or near there. cause other modules will use this function to destroy images when they want and the point that function and update product modules." Confidence: 0.75
- For image lists (main `imageUrls` and `variants[].imageUrls`), expects order values to remain a clean sequential sequence (0, 1, 2, ...) after any merge/reorder — duplicate or gapped orders are treated as a bug. When the client sends colliding order hints (e.g., an existing image with `order: 1` plus a new `{}` placeholder), the server should re-normalize orders sequentially rather than preserve the collision — "what it does is added the new image as second array element but the order was 1 1 for both, so it should keep the sequence and maintain all edge cases." Confidence: 0.65
- For e-commerce orders of variant products, expects variant-aware ordering: the client must send the chosen `variant.sku` (400 otherwise), line price and stock come from that variant (`variant.price ?? product.price`, variant stock, active offer applied on top), and a `variant: { sku, attributes }` snapshot is stored on the order line so tracking/invoice show exactly what was bought. Confidence: 0.6
- When asking how a documented API feature works operationally, wants a SHORT explanation with concrete "how to apply" steps for each operation — e.g., image updates explained as keepImages = survivors (reorderable, url backfilled), removedImageIds = delete from Cloudinary, images = new files appended, nothing sent = untouched — framed as "explain shortly how it works and to do apply update, remove, keep" — not a long write-up. Fresh evidence: immediately followed the main-images explanation with "Does it similarly works for variants images? how?" and the accepted answer mapped the same keep/remove/add operations onto the variant mechanism in the same compact style — keep/reorder via `{publicId, order}` (url backfilled), remove by omitting from the variant's `imageUrls`, add via a `{}` placeholder plus a `variantImages` file — with a short list of key differences (no Cloudinary destroy, whole-array replacement, files in `variantImages` not `images`) rather than a long write-up. Confidence: 0.7
- Expects module readmes to include a complete, ready-to-send request example for EVERY supported configuration/mode of a route — e.g., product create shown both "with variants" and "without variants", each with the exact multipart fields (`data` JSON, main `images` files, and a separate `variantImages` file field for variant photos) and how the pieces map together — not just one representative payload. Confidence: 0.6
- For sub-document array updates, expects whole-array replacement semantics to be explicit in the docs and in the assistant's explanations: sending the array (e.g., product `variants` with each variant's `imageUrls`) replaces the stored array entirely — so ALL elements must be resent, anything omitted is dropped, and not sending the field at all leaves it untouched. Contrasted with main-image updates which use explicit keep/delete lists (`keepImages`/`removedImageIds`); the variant mechanism instead treats each variant's `imageUrls` as its own full replacement list. Confidence: 0.5
- Before authorizing feature work, asks the assistant to confirm whether the capability is already implemented — "Do we need to make changes for this variants image keep? or already done?" — expecting verification against the actual code files (not answered from memory) and a definitive status report that separates what already works from deliberate design gaps (e.g., removed variant images not being destroyed on Cloudinary is a stated design choice, not a bug), with the change offered only if parity is wanted. Confidence: 0.6
- For product image management, expects removal to be complete: removing an image — main OR variant — must destroy the Cloudinary file, not just drop it from the array — explicitly framed as "keep keeps, remove removes and destroys too." Authorized closing the earlier deliberate gap (variant images were dropped from data but not deleted from Cloudinary) and expects the same destroy-on-remove semantics across both main `imageUrls` and `variants[].imageUrls`: main images not listed in `keepImages` and not re-uploaded are destroyed, variant images omitted from the new `imageUrls` are destroyed, and explicit `removedImageIds` still works. Implementation detail delegated to the assistant ("maybe we need another function for that figure it out") — a shared best-effort, deduped destroy helper in the module utils. Confidence: 0.85
- Expects order pricing to honor a product's active offerPrice: when computing line totals and the final order amount, the effective unit price (offer applied) must be used instead of the base `price` — flagged as a bug when "product has offerPrice it is not affecting the final order amount." Offer is active only when `isActive !== false` and `now` is within `startAt`/`endAt`; flat discount = price − value, percentage = price − (price × value / 100), floored at 0 — and the effective price must flow through `totalAmount`/`finalAmount`, coupon min-order checks, and the stored `unitPrice`. Confidence: 0.75
- For bug-fix rounds, wants the fix delivered WITH a report first, and defers the module documentation update until after he reviews that report — "fix that. and report to me then we will update readme file or order module" — i.e., docs for the fixed module are updated in a follow-up step the user controls, not bundled into the same change. Confidence: 0.5
- When the assistant's summary lists known trade-offs or consistency risks (e.g., Cloudinary destroy running BEFORE the DB write could leave destroyed-but-still-referenced images), the user treats them as open items to resolve rather than acceptable documented limitations — immediately asked "Two known trade off fixed? or we need to implement transaction for that where image is deleted but still referencing?" and expects the risk closed. Accepts the pragmatic fix (reorder to persist-DB-then-destroy, since a Mongo transaction can't span an external HTTP call) with the new failure modes explicitly stated (failed save = consistent; failed destroy = orphaned file, never a broken reference) over a heavyweight saga/outbox. Confidence: 0.6
ore moving to the frontend. Confidence: 0.8
e moving to the frontend. Confidence: 0.8
- For e-commerce orders, wants discounts reported as a transparent breakdown rather than a single combined number: `offerDiscount` (savings from active product `offerPrice`s) and `discount` (coupon savings only) tracked as separate order fields, with `totalDiscount = offerDiscount + discount` being what's actually subtracted from the subtotal — "we need to show both if both exists or one only if one or none." The breakdown must be exposed consistently wherever the order is displayed: API responses (create/list/track/invoice) and the invoice PDF, where each discount line ("Offer Discount" / "Coupon Discount" / "Total Discount") renders only when its value is > 0. Confidence: 0.7
- When asked to verify the design of an existing schema/module (e.g., "verify the settings module theme section, is this very well designed schema for keeping theme for whole website? just say yes or no, and if improvements needed tell me shortly. and check all types of edge cases"), expects the assistant to actually read the implementation files first (model, validation, service, controller, presets/utils) and then deliver: (1) a clear binary verdict on the design (yes/no — "yes, but with gaps" is an acceptable verdict), (2) a SHORT list of improvements, not a long write-up, and (3) a full edge-case sweep of the current implementation — e.g., full-replace PATCH wiping sibling theme fields, string-only color/font validation accepting invalid CSS, missing dark-mode completeness guard, concurrency clobbering that bypasses optimisticConcurrency via findByIdAndUpdate, in-memory-only cache staleness — with the top-priority fixes called out explicitly. Confidence: 0.7
- For preset/reset systems (e.g., multi-niche storefront settings presets), expects a clear split between "apply preset" and "full reset" semantics: applying a preset writes ONLY the theme (the look — colors/fonts/radius), leaving all content sections (brand, hero, navbar, footer, testimonials, etc.) untouched; a separate full-reset route restores every section in one write so admins can recover from accidental content loss (e.g., deleted nav links) — but on reset it must PRESERVE user-customized operational values (store currency, delivery options) when already set, falling back to preset defaults only when unset ("currency default 'usd' if already choosen currency then keep as it is"). Fresh evidence: a third "clear everything" mode (reset/empty) follows the same preservation rule — clears ALL sections to empty defaults so the admin rebuilds one by one, while still keeping currency, delivery options, and minimal necessary identity fields (brand name/niche) so the store stays usable mid-rebuild — "keep the currency as it is and if there is any conflict like we must keep some fields which are necessary keep." Confidence: 0.75
ESERVE user-customized operational values (store currency, delivery options) when already set, falling back to preset defaults only when unset ("currency default 'usd' if already choosen currency then keep as it is"). Confidence: 0.75
keep as it is"). Confidence: 0.75
- When a model field becomes redundant/superseded by a newer mechanism (e.g., product `availableColors` superseded by `colorOptions` as the single source of truth for colors), expects it fully REMOVED — not merely ignored — from the model schema, interface, validation (both create and update schemas), the filterable-fields constant, and all docs in the same pass; leaving a dead field in place is treated as an oversight ("why not removed availableColors from product model? fix that"). Confidence: 0.85
- Expects settings presets (navbar/footer links, hero CTAs, limitedOffer, etc.) and all readmes/usage guides to reference only real, existing routes — when a URL is renamed or no longer exists (e.g., `/shop` → `/products`), every occurrence must be converted in the same pass, including query-string variants (e.g., `/products?category=X`), so presets and docs never point at dead routes. Confidence: 0.8
- For multi-niche seed-data templates, expects per-niche folder organization: each niche's CSVs live in its own subfolder `collections/<niche>/` with the niche-prefixed names (`collections/clothing/clothing_users.csv` … `clothing_settings.csv`) rather than flat in the collections root — and when the output layout changes, the generator script must be updated in the same pass so regeneration reproduces the new layout (per-niche `fs.mkdirSync(..., {recursive:true})` write path), old flat files removed, and README import paths updated to match. Explicitly requested: "transform csv to there niche based into folders for example: /collcetions/clothing/all clothing csv, then update the script file." This per-niche-folder scheme supersedes the earlier flat `{niche}_{collection}.csv` layout (and before that, the folder-per-collection layout). Confidence: 0.8
- Treats each storefront niche (electronics, perfume_oil, clothing, eyewear, cosmetics, shoes, watches) as a complete, independently-usable "template" with its own full set of collection CSVs (users, brands, categories, coupons, products, orders, reviews, settings) and realistic niche-specific data (real brand/product names, BDT prices, niche-appropriate categories) — a multi-niche template library rather than one generic dataset, and expects the build to confirm ambiguities like a stated count that doesn't match the listed niches ("make 5 templates" while naming 4) before generating. Confidence: 0.75
- For navbar/settings schema design, expects role groups to avoid redundant data and stay minimal: always-visible public links live ONLY in the navbar `links` array, and the `groups` hold just role-specific actions — `auth` = Login (`/login`), `customer` = Dashboard (`/dashboard/customer`) + Logout, `admin` = Dashboard (`/dashboard/admin`) + Logout — no re-listed public links in a `public` group and no extra dashboard shortcuts (My Orders / Products / Orders / Users / Settings). Explicitly removed the `public` group because public links were already in `links`: "creates redundancy and confusion" — and expects every niche preset updated to the same minimal shape, with interface, model schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
- When the root cause of a bug lies in a SEPARATE codebase owned by another agent (e.g., the frontend project), prefers a diagnosis-only, handoff-ready workflow — explicitly said "Dont fix just find the root cause and actual viable solution then output the instructions what to do I will then send to frontend ai agent": the assistant must NOT modify the other repo, and instead deliver a definitive root-cause analysis plus a concrete, actionable fix instruction set (recommended option with verification steps) written so the user can forward it verbatim to the frontend AI agent. Confidence: 0.8
- When the user restarts a task from zero ("delete and start again"), they remove the previous artifacts themselves and expect the assistant to treat that as a clean-slate reset: verify the output folder is gone, (re)prepare the fresh plan, and continue executing immediately — "I have deleted the folder, I said to start from zero, now continue" — without re-asking already-answered questions or waiting for extra confirmation. Confidence: 0.65
- For generating MongoDB seed/sample data, prefers collection-wise CSV files — a folder per collection containing `{collection}.csv` (e.g., under `templates/database/collections/<collection>/<collection>.csv`) — with an explicit generated `_id` on every document and consistent cross-collection references so the data can be injected manually without errors ("Make sure all documents has necessary generated _id, there should be no data mistakes or bottleneck in data"). When he proposes an output location/format, he delegates the final placement choice to the assistant ("you choose the better location and report to me"). Confidence: 0.7
- Expects seed/sample data to be realistic real-world content, not generic placeholders: real product models (iPhone/Samsung/Vivo flagships), real customer names/phones/addresses, currency-aware prices (BDT), and a realistic spread of states (variant/non-variant products, paid/unpaid, COD/online, admin/customer roles) so the dataset genuinely exercises the whole project for client-side testing ("carefully generate real world data names collection documents"). Confidence: 0.7
- For generated seed data, prefers real bcrypt password hashes embedded directly in the data AND in the generator so regenerations keep them — explicitly asked to change the plaintext placeholder `12345678` to the actual hash `$2b$12$daonVNqLWBwWX.tjLKEJ.eh9/Wb8moJjGmZu2iD47yjveuA6XJMOK` for all users ("change the password 12345678 number type to string <hash>"), which makes the README's hash-on-import note obsolete. This reverses an earlier preference for plaintext placeholders hashed manually at import time. Confidence: 0.8
- For generated seed CSVs, cares that fields import with the correct data type: a numeric-looking string (e.g., the password `12345678`) must not be coerced to a number on import — he explicitly framed the fix as changing it from "number type" to a string-typed value (the bcrypt hash), so string cells must import as strings, never numbers. Confidence: 0.5
 schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
and empty-reset defaults all dropped in sync. Confidence: 0.8
ders / Products / Orders / Users / Settings). Explicitly removed the `public` group because public links were already in `links`: "creates redundancy and confusion" — and expects every niche preset updated to the same minimal shape, with interface, model schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
 schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
and empty-reset defaults all dropped in sync. Confidence: 0.8
opped in sync. Confidence: 0.8
ders / Products / Orders / Users / Settings). Explicitly removed the `public` group because public links were already in `links`: "creates redundancy and confusion" — and expects every niche preset updated to the same minimal shape, with interface, model schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
 schema, validation, and empty-reset defaults all dropped in sync. Confidence: 0.8
and empty-reset defaults all dropped in sync. Confidence: 0.8
