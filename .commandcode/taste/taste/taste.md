# Taste
- Prefers using the Codebase Memory MCP tools (index_repository, get_architecture, search_graph) for repository exploration and summarization tasks, explicitly requesting them by name. Fresh evidence: the works.md work-order rules required exploration via the codebase-memory index before implementing ("per the rules"), and the user's "follow rules strictly" directive made that mandatory for the Order/Activity work. Confidence: 0.8
- Prefers to use the code-review-agent (code-review-assistant subagent) by name to review a malfunctioning module, and expects a full written diagnostic report — root cause with file/line references, prioritized problems (Critical/High/Medium), concrete code-level fixes, implementation order, and breaking-change notes — before any code changes are made ("why not working, give me a full report of the possible solution"). Confidence: 0.7
- Expects new features/modules to strictly follow the project's documented specs and existing code conventions — repeatedly instructs "follow the rules strictly" when kicking off a new module from a spec doc — most recently appended "Follow the rules strictly." to the currency + SSL auto-fill request. Confidence: 0.97
- Maintains feature/API specs as markdown docs in the repo (e.g., src/docs/api-routes.md, src/docs/v7.md) and expects them to be read (and re-read on request) before implementation begins. Confidence: 0.8
- Expects every module in the project to have a comprehensive readme.md file (overview, how-it-works, and test data with request/response examples), including retroactively adding them to existing older modules that lack documentation. Fresh evidence: asked to "Make the setting module readme file containing module details and useage guides as other module (look for context)" — the "look for context" instruction means the assistant should read an existing module's readme first and mirror its structure (e.g., coupon.readme.md: Overview, How It Works, Test Data with per-route request/response examples, Seeding, Notes) so the new file matches sibling docs. Confidence: 0.9
- Does not want the assistant to run build/compile verification commands automatically after code changes — said "I will build when needed." Confidence: 0.8
- Communicates tersely and directively; expects immediate execution ("start working on X now", "Implement the plan") without lengthy back-and-forth once direction is given. Fresh evidence: approved the settings redesign with the two-word directive "Implement the plan" and no further commentary. Fresh evidence: explicitly asks for brief answers — "Answer shortly" — when raising design questions, expecting a concise suggestion rather than a long write-up. Fresh evidence: again ended a verification question with "answer shortly", expecting a one-line confirmation over a detailed write-up. Confidence: 0.85
- Distinguishes authentication failures from authorization failures in middleware: a valid token presented to a route the user's role isn't allowed on must yield a clear "You are not authorized!" style 401, never a misleading "Invalid token!" — expects the role check to be evaluated step-by-step per role and custom authorization errors to pass through untouched rather than being swallowed by the token-verify catch block. Confidence: 0.85
- Proactively considers data flow and parameter transmission across system boundaries (e.g., how data arrives at each endpoint between services/gateways/frontend) before considering a feature complete; asks "how will it be receiving [data X] ensure it" to verify the contract between systems. Confidence: 0.7
- When debugging multi-step integrations where data passes across system boundaries (payment gateways, APIs, webhooks), wants a systematic step-by-step trace from initiation through each stage — "Verify from starting: Init, then ... then redirect, validate etc. step by step" — rather than isolated fixes at individual endpoints. Confidence: 0.7
- When debugging issues that persist after an attempted fix, wants the assistant to first investigate, diagnose, and explain the root cause ("find and tell me that first!") before making further code changes — diagnosis before action. Confidence: 0.8
- When reporting a runtime error, wants it fixed AND the root cause explained in the same response — "fix this and let me know the issue" — i.e., the explanation should clarify why the error occurred (e.g., how Mongoose interprets a populate argument as a model name), not just list what was changed. Confidence: 0.7
- Provides IDE-context annotations (`<ide-context>`) with exact file path and line number before making requests, to give precise location context without needing to be asked. Confidence: 0.85
- Wants API route inventories documented module-by-module in the project-tracking doc (e.g., progress.md) in a specific format: module name, total route count, then each route listed with its method/type plus an inline description of what it does, and request + response formats inside code cells for every route. Expects the doc to be kept in sync as modules change — explicitly asks to "update the .md file according to the module changes" (added routes, field additions, validation notes, and the global route total all updated). Fresh evidence: again asked to "update the .md file of product module changes" right after the product module code was changed, and the resulting doc update also captured behavioral/edge-case semantics (slug uniqueness rules, isActive toggle, soft-delete 404 behavior) with notes under the route table and per-endpoint. Fresh evidence: after a round of code changes spanning multiple modules, asked to "update the .md file from our updates in Review, Product & other changed modules" — expecting the doc to be synced across all changed modules (routes, fields, behavior notes, global route totals), not just the primary one. Fresh evidence: after the user himself updated the coupon routes, asked to "update the .md file for Coupon Module" — the doc was expected to reflect the actual current route set (create/get-all/get-single/by-code/update/delete), correct the module's route total when the header was wrong (5→6), renumber section headers, and bump the global route total (52→53) so the whole doc stays internally consistent. Confidence: 0.97
- Approaches module verification in two phases: backend QA of routes first (with fixes afterward as needed), then a pass ensuring important edge cases pass for each module's routes and that code quality is readable, manageable, and reusable. Confidence: 0.7
- Is token-cost conscious; when asked to use token-saving approaches (e.g., Codebase Memory MCP), expects a quantified report of estimated tokens saved at the end of the task. Confidence: 0.7
- Prefers image/file uploads to use Cloudinary via multer middleware (`multerUpload.single()`, `parseBody`) rather than accepting raw URL strings in the request body. When adding upload support to a module, expects validation schemas to exclude fields that will be provided via file upload instead. Confidence: 0.8
- Expects the assistant to implement only what was explicitly asked for — does not want extra routes, handlers, or endpoints added without being requested. Pushes back sharply when assistant introduces unrequested functionality (e.g., separate success/fail/cancel callback routes that were not part of the original requirement). Confidence: 0.9
- Has clear architectural decisions already in mind and expects the assistant to follow that design rather than proposing or implementing alternative approaches. When a single-endpoint pattern (validate + redirect) is already established, all callbacks should flow through that same endpoint rather than being split into separate routes. Confidence: 0.8
- For Stripe integrations, prefers to maintain both PaymentIntent and Checkout Session implementations alongside each other in the codebase ("seperate both and keep both features but activate the checkout system"), keeping one active while the other is preserved for potential future use. Confidence: 0.8
- When a redirect-based payment gateway route pattern (success/cancel callback routes) was previously working and validated, prefers to restore that known-good pattern rather than redesign the integration — "the previous thing ... was working good previously add and make sure validation will work without any issue." Confidence: 0.7
- When implementing payment gateway integrations, expects webhook/callback signature verification to be included from the start — explicitly requests "make sure validate webhook works" as a requirement alongside the main implementation, not as a later add-on. Confidence: 0.6
- When a payment integration uses frontend SDK methods (e.g., Stripe PaymentIntent with confirmCardPayment), prefers the backend as a pure JSON API layer without redirect callback routes. However, when using a redirect-based gateway (e.g., Stripe Checkout Sessions, SSLCommerz), expects dedicated server-side success/cancel callback routes to handle the gateway redirect and forward the user to the frontend. Preference is integration-model-dependent, not absolute. Confidence: 0.6
- Wants compact session handoff summaries when starting a new chat, organized by fixed categories: folder structure, file naming convention, code writing style, what has been completed, what is next to do, plus "other necessary things" — important context the assistant should proactively include beyond the listed headings (e.g., known mismatches/flags, git status, recent commits). Confidence: 0.8
- Wants session handoff summaries persisted as markdown files in the project docs folder (e.g., src/docs/project-tracking/summary-cmdc-chat.md) so a new chat can be initialized with full context by reading that file — explicitly asked to "add to the summary-cmdc-chat.md file" after the summary was produced in-chat. Confidence: 0.7
- For update endpoints, prefers validation schemas where every field is optional (proper update semantics) rather than reusing create-style schemas with required fields — explicitly asked to "create another update schema where zod is optional as it's update." Confidence: 0.8
- Prefers consolidating overlapping validation schemas into a single schema per operation instead of maintaining multiple partially-duplicated schemas ("merge validation into one"). Confidence: 0.7
- Expects validation schemas, the Mongoose model, and the TypeScript interface to stay in sync within a module — fields that are validated but missing from the model/interface are treated as a bug ("model needs info details added"), and when a new property is added to a model the user explicitly expects the validations to be updated alongside it ("needs description property on the model, also update the validations"). Confidence: 0.7
- When the user proposes an approach and explicitly asks for assessment before implementing ("is this a good approach?" / "assess first if my idea is best"), wants a candid assessment first — agreement/disagreement with reasoning and edge cases (e.g., count/list mismatch, role consistency) — and explicitly wants scalability/future-maintainability addressed ("is this scalable and won't make issues?"). When the user pre-authorizes ("if so then we apply, otherwise just improve if needed"), the assistant may apply the original idea or an improved version directly after the assessment, without another confirmation round. Fresh evidence: user asked "Should we implement it, and is this scalable and won't make issues?" before committing to the payment-props design. Fresh evidence: before adding the currency field, asked "let me know should we add currency or not" — wanting a candid recommendation with reasoning (multi-client reusability) before committing. Confidence: 0.85
- Tests and verifies modules sequentially, one module at a time — declares the current module complete ("User module testing completed for now") before moving on to the next module's testing ("We have moved to Auth module testing"). Confidence: 0.6
- Expects zod request-validation schemas wired via validateRequest on every API route in a module, not just the primary ones — when asked to add a schema for one route (e.g., /auth/login), also wants the remaining routes' missing schemas covered ("insure other necessary route validation schema needed are safely added"). Confidence: 0.6
- For security edge cases (e.g., change-password where the new password must differ from the old one), wants the rule enforced in the zod schema (`.refine`) and defensively in the service layer, and expects the validation to actually catch it ("password cannot be same, validation must pass ensure these"). Confidence: 0.6
- For unique fields like slugs, prefers them optional on input: when not provided, auto-generate from the name (lowercased/hyphenated) plus a short random suffix so uniqueness is guaranteed, and expects the generator to live in a shared utils file (e.g., `src/app/utils/generateSlug.ts`) rather than inline in the model/service. Confidence: 0.8
- Explicitly asks to "maintain all types of edge cases" when implementing features — expects proactive handling of conflict/collision scenarios (duplicate names, slug collisions, uniqueness checks on create and update) during implementation, not just happy-path behavior ("Make sure all types of edge cases where maintained"); expects update operations to validate every changed field and check for conflicts (code/name conflicts, excluding self). Fresh evidence: repeated the exact instruction "Must maintain all types of edge cases" when kicking off the order module rework, and again said "Ensure all types of edge cases where maintained well" when adding payment fields/guards. Confidence: 0.95
- When fixing a bug that touches a cross-cutting concern (e.g., slug handling, soft-delete, status toggling), expects the fix to be applied consistently across the whole module — validation schema, model, interface, service logic, and read queries — not just at the spot where the error surfaced ("maintain edge cases, and update across the whole module"). Confidence: 0.6
- Expects each module to carry the full standard CRUD route set including a get-single `GET /:id` route — when a single-get route is added to one module (e.g., brand), the same pattern is expected to be replicated in subsequent modules (e.g., category) without being re-specified in detail. Confidence: 0.6
- Prefers route params to be named consistently across a module — e.g., use `couponId` on all id-based routes rather than mixing `couponId` and `couponCode` param names; explicitly said "routes should maintain consistency of params using, couponId only not couponCode." Confidence: 0.8
- Prefers soft delete over hard delete: expects an `isDeleted` boolean flag (default `false`) on the model/interface, with delete operations setting ONLY that flag and nothing else — explicitly said "Delete will only make isDeleted True" (no side effects like also deactivating `isActive`) — instead of physically removing records. Public read queries (list and get-single) filter out deleted records, which then return 404. The same applies to content hidden by other flags: public reads (including populated references) must filter to `isActive: true` only, so admin-hidden records (e.g., deactivated reviews) never leak. Confidence: 0.8
- Distinguishes admin-management reads from public-consumption reads for records with time-based validity (e.g., coupons): admin list/single views surface all non-deleted records — expired or inactive included — so admins can manage them by id, while public validation routes (e.g., get coupon by code) stay strict and reject not-yet-active/expired/inactive coupons. Only soft-deleted records are hidden from admin views. Confidence: 0.8
- Prefers toggling boolean status fields (e.g., `isActive`) through the existing generic update route (PATCH) rather than via a dedicated toggle endpoint. Confidence: 0.6
- When adding reference fields used purely for audit/ownership tracking (e.g., `createdBy`), prefers them stored as plain ObjectIds and explicitly NOT populated in API responses — "add it but not necessary to populate" — to keep responses lean and avoid unnecessary joins/lookups. Confidence: 0.7
- For moderating user-generated content (e.g., reviews), prefers a moderation flag (`isFlagged`, default false) over an active/inactive status — explicitly asked to "change the isActive with isFlagged and remove isActive property" rather than keep both. Flagged items are hidden from public reads and excluded from aggregation/rating calculations, and the admin toggle flips the flag; entity-level `isActive` fields on other models are unrelated and stay. Confidence: 0.6
- When requesting feature work on an existing module, wants a written quality/issues report on the current module FIRST — "First report me about the order module quality and issues if it has. Then..." — before any code is written, so the identified problems and the implementation plan are explicit up front. Confidence: 0.7
- For e-commerce order flows, expects server-authoritative validation and computation: products must be checked (exists, active, not deleted, sufficient stock), coupon codes verified (exists, active, in date range, meets min-order) with the discount computed per coupon rules, and monetary fields (totalAmount, discount, deliveryCharge, finalAmount) computed server-side from DB prices — never trusting client-sent unitPrice/amounts (a client could otherwise set finalAmount to 1). The update flow must follow the same strategy as create (re-validate products, re-verify coupon, recompute final amount) — "When updating order it work similar to creating route strategy." Confidence: 0.75
- For generic payment-tracking fields (sessionId/transactionId), prefers storing the RAW unprefixed gateway values — no `stripe_...`, `ssl_...`, `bkash_...` prefixes — because prefixed ids won't match the values the gateway sends back on callbacks (e.g., SSL's raw `tran_id`) and cause lookup mismatches; the `paymentProvider` field already identifies which provider was used ("keep the transactionId consistent as remove prefix as it won't match with stripe or ssl or bkash and can cause trouble and paymentProvider we can know easily what provide is used"). This reversed an earlier prefix convention. When a single generic pair can't cover a gateway's needs (e.g., SSL needs both a gateway session key and a transaction id), accepts a refined set of provider-role-specific fields (stripeSessionId, sslSessionKey, transactionId). Confidence: 0.8
- For order read endpoints (get-single order), expects authorization restricted to admin OR the order's owner — "only admin or the user who ordered can get" / "not any user." Ownership checks must include the admin exemption (e.g., `role !== "admin" && order.user.toString() !== userId`), never a bare owner-id comparison that blocks admins; and when the owner reference is populated (e.g., `populate("user", ...)`), the check must compare against the raw owner id captured before population — `toString()` on a populated doc (e.g., `"[object Object]"`) silently fails for the owner. Fresh evidence: user reiterated the requirement when the check still blocked the owner. Confidence: 0.9
- Wants multi-currency support built into the data model for reusability across international and local client projects: a shared `Currency` enum (usd, bdt, eur, gbp, inr, aed, aud, cad), products default to `usd`, and orders inherit the currency of their products (mixed-currency orders rejected), with a future store-level currency/dynamic system in mind — "this a project for our clients (international or local), reusable it should be." Confidence: 0.7
- For payment init when the order's currency is unsupported by the gateway (e.g., Stripe does not support BDT), prefers a hybrid currency strategy: charge in the order's native currency directly when the gateway supports it, otherwise auto-convert `finalAmount` to a gateway-supported currency (e.g., BDT→USD) via a free FX API (open.er-api.com, no key) at init time, round to 2 decimals, and persist the conversion metadata (`fxRate`, `fxBaseCurrency`) on the order for reconciliation/auditability — never charge a non-supported currency and never silently charge in the wrong currency. Charge amount AND charge currency are always derived from the order, never client-supplied (the Stripe init body was made empty so the client can't override currency) — "it should use the currency or convert." Chosen explicitly by the user from the "Hybrid convert" option. Confidence: 0.8
- Prefers payment-init (e.g., SSLCommerz) to auto-fill customer/shipping details from the order's user profile (name, email, phone, address, city, state, postcode, country) so the client does not have to enter those fields — none of the customer fields are required in the request body (overrides still allowed): "should be done automatically from user details so user don't need to entry those input details fill all fields relatively." Confidence: 0.7
- Prefers shared constant data (enums/static values used across multiple modules, e.g., the `Currency` enum) to live in a dedicated `src/app/constants/` folder as plain constant data, NOT as a module under `src/app/modules/` — explicitly said "dont keep currency as a module inside modules folder keep inside constants folder as constant data." The old module directory is deleted and all cross-module imports point at the constants file. Confidence: 0.9
- For major module redesigns, presents the target design up front as a detailed spec and expects a written implementation plan WITH pros and cons before any code changes — explicitly asked "Then let me know the plan. Pros and cons." after providing the singleton-settings architecture. Confidence: 0.75
- When given an external design spec (e.g., a module architecture spec pasted from Claude AI), expects it treated as guidance that must be adapted to the project's own code-writing system and naming conventions, not implemented verbatim — "must maintain current project code writing system and naming convention etc. just learn and undestand." Recognized adaptation points: plural `*.routes.ts` filenames, singular `XxxController`/`XxxServices` exports, model as `export default` via `mongoose.model<ISettings, SettingsModel>`, `PATCH` (not PUT) for update routes, `auth(UserRole.ADMIN)` from `user.interface` (no `USER_ROLE` constant file), `validateRequest` parsing only body/cookies (so `:param` enum guards become custom middleware and per-section body schemas are picked via a `Record<Section, ZodSchema>` map), `config.db_url` for seed scripts, and seed scripts under `src/scripts/` with `npx ts-node` npm scripts. Confidence: 0.8
- Prefers keeping related configuration/data areas as typed sub-documents on a single document (e.g., a singleton settings doc with `{ _id: false }` per-section schemas) rather than separate collections/modules, only promoting a section to its own collection when its access pattern genuinely diverges (needs pagination, independent versioning, or unbounded growth) — explicitly stated "not preemptively" as the rule going forward for new config areas. Fresh evidence: the settings module spec restated the rule — new config areas become new sub-schema fields on the existing model, and arrays-of-many content records (posts, products, promo banners) belong in their own module/collection, never on the config document. Confidence: 0.8
- Prefers seeding singleton/idempotent records via an explicit seed script run once at deploy time (npm script, CI-CD) rather than lazy `findOrCreate` on first request — the settings spec states seeding at deploy time avoids a race condition on duplicate `_id` and requires the script to be idempotent ("Settings singleton already exists, skipping"). Confidence: 0.8
- Prefers `optimisticConcurrency: true` on Mongoose schemas that multiple admins write to concurrently (e.g., the singleton settings doc) to guard against silent last-write-wins on concurrent edits. Confidence: 0.6
- When a module uses a cache, expects all writes to go through the service layer so cache invalidation is guaranteed — "never bypass the service layer for writes." Confidence: 0.6
- For storefront-facing config routes, expects the read endpoint (GET /settings) to be public/unauthenticated since the frontend needs it to render, while write endpoints (PUT /settings/:section) require the admin role via the existing auth middleware. Confidence: 0.7
- Prefers a minimal in-memory cache (a tiny cache module with get/set/invalidate) over adding a Redis dependency, but expects the multi-instance staleness limitation to be documented with a note to swap to Redis/pub-sub or a short TTL when scaling beyond a single instance. Confidence: 0.6
- Documents architectural rules as inline comments in source files (e.g., a `// RULE:` comment in settings.interface.ts stating that settings holds singleton site-wide config only and that list-growing content belongs in its own module/collection). Confidence: 0.5
- Shares reference/source files (e.g., theme.config.ts, global.css) for the assistant to study before planning, so the plan derives exact data shapes from them — "i have imported the theme.config.ts file, so you can learn, also global.css." Confidence: 0.6
- When a previous plan was too confusing to understand, prefers the assistant to start over from zero with a fresh, clearer plan rather than clarifying/iterating on the old one — "the previous plan of your I didn't understand so work from zero again." Confidence: 0.6
- Prefers extending an existing working approach over rewriting it — "the current approach is good but needs some changes" — expecting incremental additions (new typed sections, richer fields) that keep the established pattern intact. Confidence: 0.6
- Prefers single-sourcing shared data (e.g., brand logo stored once under `brand`, with navbar/footer referencing it instead of storing their own copies) so shared assets stay in sync — "Footer (synced logo with brand which will be used for navbar too)." Confidence: 0.6
- Expects feature/flow explanations to be written into the project-tracking `works.md` doc in a simple, numbered-step format — e.g., how the settings in-memory cache works (first request hits the DB, subsequent requests served from memory, update invalidates the cache) — including production-deployment behavior (Vercel serverless = per-instance best-effort cache, Redis only when scaling to multiple instances). Explicitly asked "Write down the flow in works.md file. simple way." Confidence: 0.6
- Extracts module documentation as a frontend-consumable data contract (e.g., settings.md) that can be handed to the frontend team directly: complete document shape, every section's exact default values as copy-paste-ready JSON seeded from the real presets (e.g., perfume_oil + theme oklch tokens), the API contract (routes, response/error shapes like the 404 "not seeded" case), and a static-defaults strategy — so the frontend can build static data mirroring the backend shape and set per-section defaults before live data arrives. Explicitly framed the doc as the thing he will "pass this document format and context to frontend." Confidence: 0.7
- Wants backend-down resilience designed and written into the docs: the frontend keeps static settings data so that if the backend fails it renders that data instead of breaking. The documented strategy should cover a short fetch timeout, silent fallback to static defaults on any failure (network/timeout/404/500 — no error screen), deep-merging live data over defaults per section so missing keys never break the UI, and a re-try interval to upgrade back to live data once the backend recovers. Confidence: 0.7
- When reviewing a data-contract document handed off to the frontend (e.g., settings.json / settings.md), expects the assistant to verify it against the actual backend implementation — schema/interface, validation, presets, and asset availability — and report which edge cases it passes plus anything still needing work (e.g., a doc default diverging from the seeded backend default like theme.dark.enabled, or /demo asset paths the backend does not actually serve) rather than accepting the document at face value. Fresh evidence: repeated the same framing nearly verbatim for settings.json — "Read the file and let me know all types of edge case passes. Anything we need to work on?" — which prompted checking asset availability and flagging the dark-mode default divergence. Confidence: 0.7
- When wrapping up / ending a phase of work (e.g., "Lets end SERVER here"), expects a final documentation close-out before closure: audit and update EVERY module readme to match the actual current code (routes, fields, behaviors — including modules not recently changed, marking any that are already current as verified), refresh the top-level usage guide, and finalize the tracking docs — progress.md (route tables/totals, QA notes marked resolved) and works.md (set an "Ended at" date and add a wrap-up entry listing what was synced and remaining known limitations). Known limitations are acceptable to leave documented for a later bug-fix round rather than fixed before closure. Fresh evidence: repeated the exact close-out trigger verbatim — "Lets end SERVER here" — with the same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
documented for a later bug-fix round rather than fixed before closure. Fresh evidence: repeated the exact close-out trigger verbatim — "Lets end SERVER here" — with the same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
he same expected workflow (update ALL module readmes per the code changes, refresh backend-usage-guide.md, finalize progress.md/works.md) before moving to the frontend. Confidence: 0.8
